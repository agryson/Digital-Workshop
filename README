This is a test for pushing to the pre-prod branch

Just as a note, to push to git :
git add .
git commit -am "MESSAGE"
git push origin BRANCH_NAME

To merge the branches:
checkout to master, then:
git merge BRANCH_TO_MERGE_WITH_MASTER
then push the changes to git

